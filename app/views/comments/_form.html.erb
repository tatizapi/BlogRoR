<%= form_with(model: [ @article, @article.comments.build ], local: true) do |form| %>
  <%= form.hidden_field :parent_id %>

  <p>
    <%= form.label :body %><br>
    <%= form.text_area :body %>
  </p>
  <p>
    <%= form.submit %>
  </p>
<% end %>
